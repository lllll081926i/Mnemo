# Mnemo 前端设计规范

本规范是 Mnemo 渲染进程的唯一视觉与布局约束。目标是让多网盘文件管理保持高信息密度、清晰边界和稳定的可读性，而不是通过卡片、渐变或多层容器制造层级。

## 1. 设计原则

- 页面以文件、任务和设置内容为中心；不为装饰增加标题、说明、统计条或空白占位。
- 浅色主题采用白色连续表面与深色文本。深色主题使用对应语义令牌，不能在组件中硬编码深灰色块。
- 信息区是平铺的连续区域，不使用“卡片中嵌卡片”的结构。
- 层级只由文字权重、间距、细分隔线、选中态和必要的侧栏定位表达。
- 同类型组件必须共享一套类名和语义令牌；页面局部 CSS 只解决内容特有的排列，不能重定义全局控件外观。
- 所有交互状态必须可见：悬停、选择、焦点、禁用、错误和危险操作不能只依赖颜色差异或透明度。

## 2. 令牌与文件职责

| 文件 | 唯一职责 | 禁止承担的职责 |
| --- | --- | --- |
| `src/assets/design-tokens.css` | 颜色、文字、边框、阴影、主题语义变量 | 布局选择器、页面组件样式 |
| `src/assets/layout-refactor.css` | 应用壳、侧栏、行式设置、列表、工具栏、Arco 控件的共用几何规则 | 业务页面特有列宽和功能逻辑 |
| `src/assets/global.css` | 尚未迁移的历史兼容样式 | 新页面的视觉实现、与公共层重复的高优先级覆盖 |
| 页面 `<style scoped>` | 该页面独有的列表列、内容排布和响应式细节 | 全局颜色、按钮、输入框、下拉框重置 |

新增样式前先判断归属。若第二个页面也需要同一规则，移入 `layout-refactor.css`；若只属于颜色或主题值，移入 `design-tokens.css`。

## 3. 颜色与文字

只使用语义变量，不直接为普通 UI 写十六进制颜色。

| 语义 | 使用场景 |
| --- | --- |
| `--bg-base` | 应用最外层背景 |
| `--bg-surface` | 页面、侧栏、列表、设置正文的连续白色表面 |
| `--bg-subtle` / `--bg-hover` | 悬停、轻微强调、非卡片式局部反馈 |
| `--text-primary` | 标题、正文、表单值、主要按钮文字 |
| `--text-secondary` | 次级信息、列表辅助列、未选中导航 |
| `--text-tertiary` / `--text-disabled` | 标签、禁用状态；不得用作正文 |
| `--color-primary` | 当前导航、焦点、主要操作、链接 |
| `--color-error` | 删除、清空、不可逆操作 |
| `--border-light` / `--border-lighter` | 页面分栏、控件外框、列表和大组分隔 |

浅色主题下，正文和可操作文字必须使用 `--text-primary` 或 `--text-link`，不得使用接近背景的浅灰色。输入框、选择器、下拉框必须有 `--control-border` 边框。

## 4. 排版与间距

- 基准字号：`13px`；辅助字号：`12px`；正文行高：`1.5`。
- 页面标题仅在设置页使用一个紧凑的 `15px` 标题。网盘、传输、分享的子页面不再重复标题。
- 侧栏菜单行高：`36px`；列表行最小高度：`44px`；通用控件高度：`32px`；工具栏按钮高度：`30px`。
- 圆角仅用于功能控件，统一为 `4px`。不使用大圆角容器、胶囊式区域或整页圆角背景。
- 页面内容使用 `--layout-page-gutter`；常规内部间距使用 6、8、12、16、20、24、32px，不随意新增近似数值。

## 5. 页面壳与响应式

### 主应用

- 顶部栏固定高度 `--layout-header-height`，只承载主导航、账户、主题、设置和窗口操作。
- 不保留底部状态/赞助/开源链接栏，也不为它预留高度。
- 主内容必须 `min-height: 0`，由内部列表或正文承担滚动，避免内容被裁切或测试页无法滚动。

### 工作区页面：网盘、传输、分享

使用 `.ui-workspace-shell`、`.ui-workspace-rail`、`.ui-workspace-content`。

- 左侧为功能导航或目录，默认宽度 `220px`，有一条细右边框。
- 右侧直接平铺工具栏、列头和列表；不能再包一层卡片或色块。
- 小于 760px 时允许侧栏和正文收窄，设置行切为单列，所有文本必须截断或换行，不能溢出。
- 网盘左栏折叠必须保留双向平滑动画，并在折叠时移除可拖动分隔条。

### 设置页面

使用 `.ui-page-shell`、`.ui-page-rail`、`.ui-page-content`、`.ui-content-column` 与 `.ui-plain-*`。

- 左栏仅做锚点定位；点击后平滑滚动到右侧对应大组，滚动时自动更新选中状态。
- 右侧是同一条连续正文，所有大组同时渲染。不得切换为互相独立的面板。
- 页面底色始终使用 `--bg-surface`；不添加卡片背景、阴影或大面积色块。
- 每个大组之间仅一条 `--border-light` 横线；组内设置行只靠 `gap` 分隔，不逐项画线。
- 常规设置行使用 `.ui-plain-row`：左边标签、右边控件；可相关的短控件可在一行排两到三个，但不得把无关设置塞入同一行。

## 6. 控件

### 按钮

- 普通按钮：白底、清晰细边框、深色或主题色文字。
- 主按钮：唯一主题色填充、反色文字；页面同一区域最多保留一个主操作。
- 文字按钮：仅用于低优先级的内联动作，悬停时有浅色背景反馈。
- 危险按钮：常态使用错误色文字与边框；悬停才使用错误色填充，避免整页出现大红块。
- 禁用状态保持边框和文字对比，不能通过极低透明度使文字不可读。

### 输入、选择与下拉

- 所有输入框、选择器、日期和数字控件统一 `32px` 高、`4px` 圆角、白色背景和可见边框。
- 表单值使用主文字色，placeholder 使用 `--control-placeholder`。
- 焦点使用主题色边框和 2px 焦点环；不使用深色投影。
- 下拉菜单、Popover 和 Modal 是平面纯色弹层，边框清晰，无玻璃模糊、无渐变、无深灰色块。
- 弹层的位置必须贴近触发控件、与边界保持可见距离；选项行高不小于通用控件高度。

## 7. 列表、工具栏与选择状态

### 文件、传输、分享列表

- 列表项仅使用底部分隔线，悬停使用 `--bg-hover`，选中使用 `--listselectbg`。
- 不为每一项添加外层卡片、阴影、独立圆角或彩色小点。
- 辅助列可在窄屏隐藏或压缩；文件名列始终 `min-width: 0` 并使用省略显示，避免挤出侧栏或右侧内容。

### 工具栏

所有网盘、传输、分享页面使用 `.toppanbtns` 与 `.toppanbtn`。

- 工具栏只保留一行；不增加重复的大标题、统计栏或空白占位行。
- 未选中列表项：只显示刷新、新建、导入、全局开始/暂停/清理等全局操作。
- 选中列表项：只显示对选中项有效的操作，例如打开、复制、暂停、删除、分享、转存、详情。
- 使用 `v-if`/`v-else` 切换互斥工具组，不使用 `v-show` 留下空的隐藏节点。
- 文件与文件夹、普通目录与回收站、不同任务状态、不同网盘能力都必须分别判断；不支持或当前模式无效的按钮不渲染。
- 搜索是中立操作，可持续显示在右侧，但不能靠空 `div` 撑开布局。

## 8. 网盘和分享能力边界

- 网盘账号切换必须是实际下拉菜单，显示当前账号、可选账号和登录入口；不能用模拟点击或仅文本提示代替。
- 未登录时，回收站、全盘搜索和目录区域显示明确的登录后提示。
- 分享侧栏仅展示当前网盘实际支持的分组和页面：分享列表、导入链接、历史导入分别受后端 API 能力控制。
- 当账号切换使当前分享子页不可用时，自动回退到第一个可用子页；完全不支持时显示单一明确空态。
- 创建分享、查看分享、导入和历史列表的能力必须根据真实 provider API 收口，禁止页面分别维护互相矛盾的判断。

## 9. 无障碍与交互

- 所有可点击控件必须有可见 `:focus-visible` 状态，键盘可访问。
- 有关闭逻辑的页面必须支持显式关闭和 Escape：设置关闭后返回进入前的主页面。
- 动画只用于表达空间关系，例如网盘侧栏收折；默认时长约 220ms，并尊重 `prefers-reduced-motion`。
- 文本、图标和状态不得仅依赖色彩传达含义；危险操作保留文字标签。

## 10. 明确禁止

- 卡片式页面、卡片嵌套、背景叠层、玻璃拟态、背景模糊、深灰色大色块、彩虹渐变。
- 在页面局部用 `!important` 覆盖已经由公共层定义的按钮、输入、侧栏、工具栏或弹层规则。
- 重复的页面标题、说明文字、无内容的统计条、装饰性小点、空的 spacer 节点。
- 每个设置项都画横线；设置页仅允许大组之间分隔。
- 用固定宽度破坏侧栏收缩或窄屏布局；所有弹性区域都必须带 `min-width: 0`。
- 以 `v-show` 隐藏功能上不应存在的操作；这种情况使用 `v-if`。

## 11. 新页面检查清单

提交前逐项确认：

- 是否只引用了语义令牌，而不是硬编码普通颜色？
- 是否复用了公共壳、行式设置、工具栏和控件规则？
- 是否没有卡片容器、深色块、重复标题或多余说明？
- 是否在浅色主题下保证正文、标签、输入值和按钮的深色可读性？
- 是否为未选中、选中文件、选中文件夹、特殊模式和不支持 provider 分别控制操作？
- 是否在窄宽度、长文件名、空列表、未登录和不支持能力时仍可读可用？
- 是否通过 `vue-tsc`、相关 Vitest 测试和 `git diff --check`？
