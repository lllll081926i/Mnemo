# S3 功能详情

> 调研范围：`internal/drive/providers/s3/s3.go`
> 对照旧版：`../Mnemo/src/utils/s3Client.ts`
> 整体完成度：⚠️ ~70%

---

## 能力声明（s3.go:20-37）

```
mountedStorage, permanentDelete: true
recycleBin, trashView: false
UploadMode: direct
```

---

## 1. 连接配置（s3.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| endpoint + AK/SK | ✅ | `s3.go:55-80` connOf 从 c.Token.Conn 取 | 无（Username/Password 映射 AK/SK） |
| 连接持久化 | ✅ | account.go Token.Conn | 无 |
| **forcePathStyle** | ❌ | `s3.go:71` UsePathStyle:true 硬编码 | 🔴 旧版可配置，对 AWS 原生 endpoint 可能不兼容 |
| **sessionToken** | ❌ | `s3.go:70` 第三参数固定空 | 🟡 旧版支持临时凭证 |
| BasePath 前缀 | ✅ | `s3.go:58-62` normalizePrefix | 无 |

---

## 2. 文件列表（s3.go）

✅ `s3.go:95-136` ListObjectsV2 + Delimiter:/ 模拟虚拟目录。CommonPrefixes→目录，Contents→文件。

> 🟡 无 seenContinuationTokens 防重复游标保护。

---

## 3. 下载（s3.go）

✅ `s3.go:160-182` GetDownloadURL PresignGetObject 预签名 URL 4h redirect。

---

## 4. 上传（s3.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| direct PUT | ✅ | `s3.go:225-243` client.PutObject 单次 PUT | 🟡 无 multipart（旧版 lib-storage 自动多段） |
| **冲突策略** | ❌ | 无 resolveProviderPathConflictAction | 🔴 直接覆盖 |
| **进度回调** | ❌ | 无 | 🟡 旧版 httpUploadProgress |
| **递归目录上传** | ❌ | 仅单文件 | 🟡 旧版递归 |

---

## 5. 文件操作（s3.go）

| 操作 | 状态 | Go 证据 | 差距 |
|------|:----:|---------|------|
| Mkdir | ✅ | `s3.go:184-192` PutObject key 加 / 0 字节 | 无 |
| Rename | ⚠️ | `s3.go:194-206` copyObj + deleteObj | 🔴 **仅单对象不递归目录** |
| Move | ⚠️ | `s3.go:214-227` copyObj + deleteObj | 🔴 **不递归目录** |
| Copy | ⚠️ | `s3.go:229-243` CopyObject | 🔴 **不递归目录，无大文件分片复制(>5GB)** |
| Delete | ⚠️ | `s3.go:208-216` deleteObj 逐个 | 🔴 **不递归目录，无批量删除** |

> 🔴 **s3 的 Rename/Move/Copy/Delete 均仅处理单对象，不递归目录子对象**。旧版对目录操作先 listAllS3Objects 再逐个/批量处理。

---

## 6. 回收站 / 分享 / 搜索

❌ 均无（设计）。永久删除。

---

## P0 差距清单

1. 🔴 **forcePathStyle 硬编码不可配置**（`s3.go:71`）
2. 🔴 **目录递归操作完全缺失**：Rename/Move/Copy/Delete 仅处理单对象，不递归目录子对象
3. 🔴 **上传冲突策略完全缺失**
4. 🟡 sessionToken 不支持
5. 🟡 无大文件分片复制（旧版 >5GB 走 CreateMultipartUpload+UploadPartCopy）
6. 🟡 无批量删除（旧版 DeleteObjects 1000/批）
7. 🟡 上传无 multipart、无进度回调、无递归目录上传
8. 🟡 分页游标无防重复保护
9. 🟡 S3 CopySource 不编码
