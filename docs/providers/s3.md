# S3 功能详情

> 调研范围：`internal/drive/providers/s3/s3.go`
> 对照旧版：`../Mnemo/src/utils/s3Client.ts`
> 整体完成度：✅ ~90%

---

## 能力声明（s3.go:20-37）

```
mountedStorage, permanentDelete: true
recycleBin, trashView: false
UploadMode: direct
```

---

## 1. 连接配置（s3.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| endpoint + AK/SK | ✅ | `s3.go:55-80` connOf 从 c.Token.Conn 取 | 无（Username/Password 映射 AK/SK） |
| 连接持久化 | ✅ | account.go Token.Conn | 无 |
| **forcePathStyle** | ✅ | `s3.go:86-90` `*bool` 可配置，`ForcePathStyle` 非空时按值 | 无 |
| **sessionToken** | ✅ | `s3.go:94` `cc.SessionToken` 传入临时凭证 | 无 |
| BasePath 前缀 | ✅ | `s3.go:58-62` normalizePrefix | 无 |

---

## 2. 文件列表（s3.go）

✅ `s3.go:95-136` ListObjectsV2 + Delimiter:/ 模拟虚拟目录。CommonPrefixes→目录，Contents→文件。

> 🟡 无 seenContinuationTokens 防重复游标保护。

---

## 3. 下载（s3.go）

✅ `s3.go:160-182` GetDownloadURL PresignGetObject 预签名 URL 4h redirect。

---

## 4. 上传（s3.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| direct PUT | ✅ | `s3.go:225-243` client.PutObject 单次 PUT | 🟡 无 multipart（旧版 lib-storage 自动多段） |
| **冲突策略** | ✅ | `s3.go:506-518` ConflictPolicy refuse/rename/overwrite | 无 |
| **进度回调** | ✅ | `s3.go:524` NewProgressReader 实时 DownSize/DownProcess | 无 |
| **递归目录上传** | ❌ | 仅单文件 | 🟡 旧版递归 |

---

## 5. 文件操作（s3.go）

| 操作 | 状态 | Go 证据 | 差距 |
|------|:----:|---------|------|
| Mkdir | ✅ | `s3.go:184-192` PutObject key 加 / 0 字节 | 无 |
| Rename | ✅ | `s3.go:322` copyRecursive + deleteRecursive | 无 |
| Move | ✅ | copyRecursive + deleteRecursive | 无 |
| Copy | ✅ | `s3.go:492` copyRecursive | 无 |
| Delete | ✅ | `s3.go:365-396` deleteRecursive（DeleteObjects 1000/批） | 无 |

> ✅ **s3 的 Rename/Move/Copy/Delete 均递归处理目录子对象**。`listAllUnder`（`s3.go:339-360`）递归列出，`deleteRecursive`（`s3.go:365-396`）批量删除 1000/批，`copyRecursive`（`s3.go:400-492`）逐对象复制。

---

## 6. 回收站 / 分享 / 搜索

❌ 均无（设计）。永久删除。

---

## P0 差距清单

1. ✅ **forcePathStyle 可配置**（`s3.go:86-90` `*bool`）
2. ✅ **目录递归操作已实现**：`listAllUnder`+`deleteRecursive`+`copyRecursive`（`s3.go:339-492`）
3. ✅ **上传冲突策略已实现**（`s3.go:506-518` ConflictPolicy）
4. ✅ **sessionToken 支持**（`s3.go:94`）
5. ✅ **批量删除已实现**（`s3.go:388` DeleteObjects 1000/批）
6. ✅ **进度回调已实现**（`s3.go:524` NewProgressReader + DownSize/DownProcess）
7. 🟡 无大文件分片复制（旧版 >5GB 走 CreateMultipartUpload+UploadPartCopy）
8. 🟡 上传无 multipart、无递归目录上传
9. 🟡 分页游标无防重复保护
10. 🟡 S3 CopySource 不编码
