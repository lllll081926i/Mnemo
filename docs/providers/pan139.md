# 139 云盘（pan139）功能详情

> 调研范围：`internal/drive/providers/pan139/pan139.go`（单文件，~981 行）
> 对照旧版：`../Mnemo/src/pan139/` + `src/drive/providers/pan139.ts`
> 整体完成度：✅ ~90%

---

## 能力声明（pan139.go:44-51）

```
copy, recycleBin, permanentDelete, trashRestore: true
```
> trashView 未声明（默认 false，与旧版一致）

---

## 1. 登录（pan139.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| 账密登录 | ✅ | `pan139.go:799-898` loginByPassword mail.10086.cn 三步流程 | 无 |
| Authorization 直登 | ✅ | `pan139.go:752-770` authLogin | 无 |
| mail.10086.cn RMKEY/getArtifact/authorize | ✅ | `pan139.go:806-893` 完整三步 | 无 |
| token 刷新 | ✅ | `pan139.go:232-253` refreshAuthorization 15 天阈值 | 无 |
| 自动刷新 | ✅ | `pan139.go:296` loadCred 自动调 refreshAuthorization | 无 |

---

## 2. 文件列表（pan139.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| ListPage | ✅ | `pan139.go:399-419` POST /file/list nextPageCursor | 无 |
| ListPaged | ✅ | `pan139.go:588-594` | 无 |
| **分页参数推进** | ⚠️ | `pan139.go:408-410` pageNum=1 startNumber=0 固定 | 🟡 未根据 marker 推进，多页场景可能只返回第一页 |

---

## 3. 下载（pan139.go）

✅ `pan139.go:566-576` GetDownloadURL POST /file/getDownloadUrl，proxy + Referer + Concurrency=1。对齐。

---

## 4. 视频预览（pan139.go）

✅ `pan139.go:578-585` GetVideoPreview 复用 GetDownloadURL origin 原画。

---

## 5. 上传（pan139.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| 分片上传 | ✅ | `pan139.go:684-710` UploadOneFile chunkSize=5MB base64 | 无 |
| uploadChunk | ✅ | `pan139.go:712-736` POST /file/upload uploadId 串行 | 无 |
| 进度回调 | ✅ | DownSize/DownProcess 实时 | 无 |

> 139 上传是 base64 编码分片串行上传，无断点续传（旧版同样无）。

---

## 6. 文件操作（pan139.go）

| 操作 | 状态 | Go 证据 |
|------|:----:|---------|
| Mkdir | ✅ | `pan139.go:495-505` /file/createCatalog + `:627-629` MkdirR |
| Rename | ✅ | `pan139.go:507-517` /file/rename |
| Move | ✅ | `pan139.go:655-666` /file/move |
| Copy | ✅ | `pan139.go:668-679` /file/copy |

---

## 7. 回收站（pan139.go）

| 操作 | 状态 | Go 证据 |
|------|:----:|---------|
| Trash | ✅ | `pan139.go:635-643` /file/trash |
| Delete | ✅ | `pan139.go:645-655` /file/delete |
| Restore | ✅ | `pan139.go:657-665` /file/restore |

> trashView 未声明（无法查看回收站列表，但支持 trash/delete/restore 三项操作）。

---

## 8. RefreshAccount（pan139.go）

✅ `pan139.go:738-756` RefreshAccount POST /file/getDiskInfo 更新 usedSize/totalSize。对齐。

---

## 9. 加密/签名（pan139.go）

| 原语 | 状态 | Go 证据 |
|------|:----:|---------|
| AES-CBC | ✅ | `pan139.go:86-108` iv‖ciphertext base64 PKCS7 |
| AES-ECB 解密 | ✅ | `pan139.go:111-126` hex 密文 |
| calSign | ✅ | `pan139.go:155-162` encodeURIComponent → sort → base64 → md5 组合 |
| sortedJSONStringify | ✅ | `pan139.go:139-153` 递归排序 key |

---

## 10. ProvideHashes

❌ 未声明（旧版 pan139 也未声明，一致）。pan139 上传未实现独立秒传接口。

---

## 差距清单

1. 🟡 **ListPage 分页参数未推进**：pageNum/startNumber 固定 1/0，多页场景可能失效。这是最显著的实现差距。
2. 其余功能与旧版对齐，无重大缺失。
