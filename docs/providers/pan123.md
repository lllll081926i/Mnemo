# 123 云盘（pan123）功能详情

> 调研范围：`internal/drive/providers/pan123/`（pan123.go + upload.go）
> 当前证据：本仓库 `internal/drive/providers/pan123`、统一能力注册表和本地自动化测试；本次未读取父目录旧项目。
> 状态：✅ 统一驱动已注册；可靠性以自动验证范围和已知限制为准，见 [Provider 状态总表](../PROVIDER_STATUS.md)。

---

## 能力声明（pan123.go:54-76）

```
search, createShare, shareExpiration, sharePassword, shareHistory, importShare: true
trashView, trashRestore: true
permanentDelete: true
copy: false
SetHashes(["md5"], ["md5"])
```

---

## 1. 登录（pan123.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| 账密登录 | ✅ | `pan123.go:350-382` pan123Login，区分邮箱/手机并兼容数字/字符串 code | 无 |
| refresh_token 存储 | ✅ | `pan123.go:384-438` authLogin 保存 JSON {username,password}，登录后强校验用户信息 | 无 |
| 401 自动重登 | ✅ | `pan123.go:279-305,321-337` api + storedPan123Credentials，复用账密重登 | 无 |
| RefreshAccount | ✅ | `pan123.go:441-472` apiUserInfo 更新配额；失效且重登失败时返回错误 | 无 |

> 说明：业务 API 的 `code` 同时按数字和数字字符串解析，保持与旧版 `Number(json.code)` 一致；透明重登后的 token 由 `drive` 门面在请求结束时持久化到对应账号，不会串到其他账号。

---

## 2. 文件列表（pan123.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| List | ✅ | `pan123.go:933-940` fileListRaw + mapFiles | 无 |
| ListPaged | ✅ | `pan123.go:942-953` fileListPageRaw + 数字页 marker | 无 |
| ListTrash | ✅ | `pan123.go:955-961` fileListRaw(trashed=true) | 无 |

---

## 3. 下载（pan123.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| AList Link 解析 | ✅ | `pan123.go:1031-1193` resolveAListFile + alistLink | 无 |
| 下载信息 | ✅ | `pan123.go:1195-1220` proxy + ForceLocalProxy | 无 |
| 过期时间解析 | ✅ | `upload.go:466-490` getExpiresTime 多格式 | 无 |
| **点击快照 pin** | ✅ | `App.PinFileSnapshot` + `transfer/manager.go` 写入统一 file cache；`pan123.go:1031-1050` 优先恢复 | 无 |
| **description 备份回读** | ✅ | `pan123.go:635-648,1031-1050` decode `pan123meta` | 无 |
| Concurrency | 1 | `pan123.go:1214-1217` | 无（旧版一致） |

---

## 4. 视频预览（pan123.go）

✅ `pan123.go:1222-1236` GetVideoPreview 复用 GetDownloadURL，单 origin 质量；下载和预览共用点击快照恢复链路。

---

## 5. 上传（upload.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| MD5 计算 | ✅ | `upload.go:133-144` fileMD5 | 无 |
| upload_request | ✅ | `upload.go:363-395` 通过 `duplicateFromPolicy` 映射 overwrite→2、非覆盖策略→1；初始化结果持久化 | 123 API 只有 duplicate=1/2，skip 与 rename 共用非覆盖分支 |
| 秒传命中 | ✅ | `upload.go:373-380` Reuse/Key=="" | 无 |
| 分片计划 | ✅ | `upload.go:40-55` calcPan123ChunkPlan ≤16MB/片 | 无 |
| presignBatch | ✅ | `upload.go:147-175,214-238` 多片/单片；失效会话可触发重建 | 无 |
| putChunk | ✅ | `upload.go:178-192,268-289` PUT 分片、严格接受 2xx、403 整批重签 | 无 |
| complete | ✅ | `upload.go:416-438` upload_complete/v2 回退 v1 | 无 |
| 进度回调 | ✅ | `upload.go:248-295,435-437` DownSize/DownProcess 实时 | 无 |
| 停止 | ✅ | `upload.go:226-241,444-459` ctx.Err + IsStop | 无 |
| **断点续传持久化** | ✅ | `upload.go:72-115,343-410` 会话包含远端 UploadId；键包含 MD5；失效会话清理后只重建一次 | 无 |

---

## 6. 秒传（pan123.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| etagAsMd5 | ✅ | `pan123.go:1455-1464` 32 位 hex | 无 |
| RapidUploadByHash | ✅ | `pan123.go:1475-1503` upload_request etag | 无 |
| ResolveTransferHash | ✅ | `pan123.go:1505-1520` detail 取 Etag | 无 |

---

## 7. 文件操作（pan123.go）

| 操作 | 状态 | Go 证据 | 差距 |
|------|:----:|---------|------|
| Mkdir | ✅ | `pan123.go:1238-1255` upload_request type:1 | 无 |
| Rename | ✅ | `pan123.go:1257-1274` /file/rename | 无 |
| Move | ✅ | `pan123.go:1324-1338` batchOp /file/mod_pid | 无 |
| Copy | ❌(设计) | `pan123.go:1340-1343` 返回空数组 | 无（caps copy:false） |

---

## 8. 回收站（pan123.go）

| 操作 | 状态 | Go 证据 | 差距 |
|------|:----:|---------|------|
| Trash | ✅ | `pan123.go:1287-1296` /file/trash operation:true | 无 |
| Restore | ✅ | `pan123.go:1298-1307` /file/trash operation:false | 无 |
| Delete | ✅ | `pan123.go:1309-1322` /file/delete | 无 |
| ListTrash | ✅ | `pan123.go:955-961` | 无 |
| **TrashPurge** | — | 声明已移除（Caps 不再声明 trashPurge） | — |
| **TrashClear** | — | 声明已移除（Caps 不再声明 trashClear） | — |

---

## 9. 分享（pan123.go）

| 子功能 | 状态 | Go 证据 | 差距 |
|--------|:----:|---------|------|
| CreateShare | ✅ | `pan123.go:1394-1453` /share/create | 无 |
| 过期时间 | ✅ | `pan123.go:1347-1392` formatPan123Expiration | 无 |
| 密码 | ✅ | sharePwd | 无 |
| **分享导入 importShare** | ✅ | `pan123.go:1535-1618` ImportShareSession + SaveShare | 无 |

---

## 10. 搜索（pan123.go）

✅ `pan123.go:963-977` fileListRaw search=keyword。对齐。

---

## P0 差距清单

1. ✅ **断点续传持久化已实现**（`upload.go:342-410` 保存远端会话与已完成分片；同名替换文件按 MD5 隔离；失效会话只自动重建一次）
2. ✅ **分享导入已实现**（`pan123.go:1402-1405` ImportShareSession + SaveShare）
3. ✅ **TrashPurge/TrashClear 声明已移除**（Caps 不再声明 trashPurge/trashClear）
4. ✅ **下载/预览点击快照恢复**：统一 file cache + `pan123meta` 已覆盖旧版 pendingLink 与 description 回读语义
5. ✅ **下载参数兼容**：`alistLink` 支持标准/无 padding/URL-safe Base64，兼容旧版 `Buffer.from(..., "base64")`
6. ✅ **上传冲突映射**：`ConflictPolicy` 不再固定覆盖，默认保持旧行为，非覆盖策略映射为 123 的 `duplicate=1`
